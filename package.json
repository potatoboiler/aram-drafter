{
  "name": "aram-drafter",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://potatoboiler.github.io/aram-drafter",
  "dependencies": {
    "@octokit/rest": "^22",
    "dotenv": "^17",
    "react": "^19",
    "react-dom": "^19",
    "vite": "^7"
  },
  "scripts": {
    "start": "vite start",
    "build": "vite build",
    "deploy": "gh-pages -d build",
    "fetch": "node scripts/fetch.js"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
